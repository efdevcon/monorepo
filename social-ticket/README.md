# social-ticket (legacy)

Devcon SEA social share image generator. Mostly superseded: session/AV/personal-schedule cards are now generated by the devcon website (`devcon/src/pages/api/social/*`). Only the personal attendee ticket card (`/[name]`) is still served from here.

Status and deletion plan: [docs/notes.md](./docs/notes.md)

```bash
pnpm install   # from the repo root
pnpm dev
```
