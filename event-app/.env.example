# DevaBot RAG backend (devcon-ai service, see ../devcon-ai)
NEXT_PUBLIC_DEVABOT_API_URL=http://localhost:3030

PRETALX_API_KEY=your_pretalx_api_key

# Devcon API (data provider) — NEXT_PUBLIC_ so it's available in the browser.
# Use the hosted API, or http://localhost:4000 for a local devcon-api.
NEXT_PUBLIC_DEVCON_API_URL=https://api.devcon.org
# Default dataset shown when no ?dataset param is set. One of:
# test-devcon-8 | devcon8 | devcon-7. Unset falls back to devcon-7.
# All three remain selectable at runtime via the debug panel regardless.
NEXT_PUBLIC_DEVCON_API_EVENT_ID=devcon-7

# Debug panel (dataset switcher + time mocking). Auto-on in local dev. In
# production set to "true" to always show it, or just append ?debug=1 to any URL.
# Do NOT enable it by setting NODE_ENV=development (that breaks the prod build).
NEXT_PUBLIC_ENABLE_DEBUG=false

# Preview mode: when "true", the clock starts at the (default) dataset's
# conference start on initial load, so schedule/live/today reflect the event
# without manual ?mockNow. Leave "false" on the real live-event deploy, or the
# clock would be pinned to the event start. Explicit ?mockNow always overrides.
NEXT_PUBLIC_MOCK_NOW_TO_EVENT_START=false

# Supabase (email-OTP login)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase service role key — server-only, used by /api/auth/bridge to
# generate a fresh magic link for the signed-in user at first-launch time,
# so installing the PWA carries their session into the installed app. Same
# project as above.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Signs the install-bridge token (src/app/api/_lib/bridgeToken.ts) — any
# random value works (e.g. `openssl rand -hex 32`), not shared with anything
# else. Without this the install-bridge feature quietly no-ops.
INSTALL_BRIDGE_SECRET=your_random_signing_secret

# Pretix (ticket fetching) — server-only, used by /api/tickets
PRETIX_API_URL=https://ticketh.xyz
PRETIX_ORGANIZER=your_organizer_slug
PRETIX_EVENT=your_event_slug
PRETIX_EVENT_NAME=Devcon
PRETIX_API_KEY=your_pretix_api_key

# Announcements (Notion-authored, /api/announcements/*) — server-only.
# Notion internal-integration token; the integration must be connected to the
# announcements database (Notion DB page > ... > Connections).
NOTION_SECRET=your_notion_integration_secret
# Optional override; defaults to the shared events-workspace announcements DB.
# NOTION_ANNOUNCEMENTS_DB_ID=

# Web push: generate a pair with `npx web-push generate-vapid-keys`.
# The SAME keypair must be used everywhere (a new pair orphans all existing
# subscriptions). Public key is safe to expose; private key is not.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
# Optional contact for push services (defaults to the devcon website mailto).
# VAPID_SUBJECT=mailto:you@example.org
# Gate for /api/push/dispatch (the scheduled sender). Any long random string.
PUSH_DISPATCH_SECRET=your_random_dispatch_secret
