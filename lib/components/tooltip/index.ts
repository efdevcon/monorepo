// TODO: Nextjs doesn't allow global styling import at this level  - to use tippy you'll have to add the css manually in the consuming project
// import "react-tippy/dist/tippy.css";
import { Tooltip } from "react-tippy";

export default Tooltip;
