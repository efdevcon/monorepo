# Devconnect App (legacy)

Conference app used at Devconnect ARG @ [app.devconnect.org](https://app.devconnect.org/). Kept for archival purposes; new event work happens in [event-app](../event-app/).

```bash
pnpm install   # from the repo root
pnpm dev       # http://localhost:3000
```
