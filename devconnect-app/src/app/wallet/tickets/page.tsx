'use client';
import WalletPageContent from '../WalletPageContent';

export default function TicketsPage() {
  return <WalletPageContent title="Tickets" showTabs={true} activeTabHref="/wallet/tickets" />;
}
