/** Ticket store configuration — single source of truth for store-wide settings */

export const STORE_CONFIG = {
  /** Percentage discount applied when paying with crypto (e.g. 3 = 3%) */
  cryptoDiscountPercent: 3,
}
