atproto-slurper I'd just delete, its a server that ingests devconnect events via atprotocol - lives on render

the only thing it feeds is the devconnect website and app, but if you're done with Devconnect, I'd either just delete those/take them down/redirect them to Devcon, or if you want it to stay around for archival purposes, snapshot the current data and just load it as JSON, then delete this repo

if the server is still running, feel free to delete it
