# lib

hosts any code that should be reused across project boundaries

this could be relevant for the website and the app depending on what you do

it uses pnpm workspaces to make "lib" installable as a package from the other projects
